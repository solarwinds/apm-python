[tox]
skipsdist = True
skip_missing_interpreters = True
envlist =
  py36
  py37
  py38
  py39
  py310

[testenv]
allowlist_externals = echo
changedir = tests
deps =
  -rdev-requirements.txt
commands_pre =
  pip install -Ie {toxinidir}
commands =
  pytest {posargs}